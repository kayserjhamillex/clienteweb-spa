import { Title } from '@angular/platform-browser';

export interface Persona {
  id?: number,
  Fullname?: string,
  Numerodocumento?: string,
  Email?: string,
  Password?: string,
  ImagenPeople?: string,
  Google?: string
}
